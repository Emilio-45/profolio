# Emilano Rios — Static Portfolio

Simple static portfolio site. Open `index.html` in a browser to view.

To serve locally (Python 3):

```bash
cd profolio
python -m http.server 8000
# then open http://localhost:8000
```
